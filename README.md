# IrishAutoDubber
This script is a Python base console application designed to dub an input video into Irish audio using paired English and Irish subtitle files (`eng.srt` and `gael.srt`). It uses Selenium to automate speech synthesis on the Abair.ie, generating dubbed audio segments that align with subtitle timings, and then muxes the new audio into an output video
